[package]
name = "salvo-cms"
version = "0.1.0"
edition = "2021"

[dependencies]
salvo = { version = "0", features = ["anyhow"] }
tokio = { version = "1", features = ["macros"] }

anyhow = "1"
thiserror = "2.0"

tracing = "0.1"
tracing-subscriber = "0.3"

serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"